# etc

This directory contains files that belong under `/etc` on a Linux system (e.g., `pacman.conf`, `pkglist.txt`, `pkglist-aur.txt`, and the `hooks/` directory). Copy or symlink them into place as needed.
